# Local backend API
NEXT_PUBLIC_API_URL=http://localhost:5000

# Production example:
# NEXT_PUBLIC_API_URL=https://your-backend-domain.com

# Optional public payment key, only needed if Razorpay checkout is enabled in frontend
NEXT_PUBLIC_RAZORPAY_KEY=
